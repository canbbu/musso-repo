
<template>
  <div class="sidebar">
    <div class="sidebar-header">
      <h1 class="sidebar-title">PlayerHub</h1>
    </div>
    
    <div class="sidebar-user">
      <div class="user-avatar">
        <img src="https://i.pravatar.cc/100" alt="User avatar" />
      </div>
      <div class="user-info">
        <p class="user-name">{{ userName }}</p>
        <p class="user-role">{{ userRole }}</p>
      </div>
    </div>
    
    <nav class="sidebar-nav">
      <nuxt-link to="/dashboard" class="nav-link">
        <span class="nav-icon">📊</span>
        <span>Dashboard</span>
      </nuxt-link>
      
      <nuxt-link to="/matches" class="nav-link">
        <span class="nav-icon">⚽</span>
        <span>Matches</span>
      </nuxt-link>
      
      <nuxt-link to="/stats" class="nav-link">
        <span class="nav-icon">📈</span>
        <span>Player Stats</span>
      </nuxt-link>
      
      <nuxt-link to="/community" class="nav-link">
        <span class="nav-icon">👥</span>
        <span>Community</span>
      </nuxt-link>
      
      <nuxt-link to="/gallery" class="nav-link">
        <span class="nav-icon">🖼️</span>
        <span>Gallery</span>
      </nuxt-link>
      
      <nuxt-link v-if="isAdmin" to="/finance" class="nav-link">
        <span class="nav-icon">💰</span>
        <span>Finance</span>
      </nuxt-link>
    </nav>
    
    <div class="sidebar-footer">
      <button class="btn-logout" @click="logout">
        <span class="logout-icon">🚪</span>
        <span>Logout</span>
      </button>
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue';
import { useRouter } from 'vue-router';

const router = useRouter();
const userName = ref('John Doe');
const userRole = ref('Player');
const isAdmin = ref(false);

onMounted(() => {
  // In a real app, get this from your auth store
  const role = localStorage.getItem('user-role');
  if (role === 'admin') {
    userRole.value = 'Admin';
    isAdmin.value = true;
  }
});

const logout = () => {
  // Clear auth data
  localStorage.removeItem('user-role');
  // Redirect to login
  router.push('/login');
};
</script>

<style>
.sidebar {
  width: 250px;
  height: 100vh;
  background-color: var(--primary-color);
  color: white;
  display: flex;
  flex-direction: column;
  position: fixed;
  left: 0;
  top: 0;
}

.sidebar-header {
  padding: 20px;
  text-align: center;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.sidebar-title {
  margin: 0;
  font-size: 1.5rem;
  font-weight: 600;
}

.sidebar-user {
  padding: 20px;
  display: flex;
  align-items: center;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.user-avatar img {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  object-fit: cover;
}

.user-info {
  margin-left: 10px;
}

.user-name {
  margin: 0;
  font-weight: 500;
}

.user-role {
  margin: 0;
  font-size: 0.8rem;
  opacity: 0.8;
}

.sidebar-nav {
  flex: 1;
  padding: 20px 0;
  overflow-y: auto;
}

.nav-link {
  display: flex;
  align-items: center;
  padding: 12px 20px;
  color: white;
  text-decoration: none;
  transition: background-color 0.2s;
}

.nav-link:hover,
.nav-link.router-link-active {
  background-color: rgba(255, 255, 255, 0.1);
}

.nav-icon {
  margin-right: 10px;
  font-size: 1.2rem;
}

.sidebar-footer {
  padding: 20px;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
}

.btn-logout {
  width: 100%;
  padding: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  background-color: rgba(255, 255, 255, 0.1);
  border: none;
  border-radius: 4px;
  color: white;
  cursor: pointer;
  transition: background-color 0.2s;
}

.btn-logout:hover {
  background-color: rgba(255, 255, 255, 0.2);
}

.logout-icon {
  margin-right: 8px;
}

@media (max-width: 768px) {
  .sidebar {
    width: 70px;
  }
  
  .sidebar-title,
  .user-info,
  .nav-link span:last-child,
  .btn-logout span:last-child {
    display: none;
  }
  
  .nav-icon,
  .logout-icon {
    margin-right: 0;
    font-size: 1.5rem;
  }
  
  .nav-link,
  .btn-logout {
    justify-content: center;
  }
  
  .sidebar-user {
    justify-content: center;
  }
}
</style>
